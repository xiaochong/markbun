export type { MenuItemConfig, MenuConfig } from '@/shared/types';

export interface AppMenuState {
  showSidebar: boolean;
  showTitleBar: boolean;
  showToolBar: boolean;
  showStatusBar: boolean;
  sourceMode: boolean;
}
